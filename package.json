{
  "name": "@openhoo/npm-library-template",
  "version": "0.1.0",
  "packageManager": "bun@1.3.14",
  "description": "Template repository for OpenHoo Bun-powered TypeScript npm libraries.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/openhoo/npm-library-template.git"
  },
  "bugs": {
    "url": "https://github.com/openhoo/npm-library-template/issues"
  },
  "homepage": "https://github.com/openhoo/npm-library-template#readme",
  "publishConfig": {
    "registry": "https://registry.npmjs.org",
    "access": "public"
  },
  "type": "module",
  "license": "MIT",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    }
  },
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --dts --sourcemap --clean",
    "biome:check": "biome ci --error-on-warnings .",
    "biome:fix": "biome check --write .",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "lint": "biome lint .",
    "lint:fix": "biome lint --write .",
    "test": "bun test",
    "test:coverage": "bun test --coverage",
    "typecheck": "tsc --noEmit",
    "check": "bun run biome:check && bun run typecheck && bun run test:coverage && bun run build",
    "hooks:install": "if git rev-parse --git-dir >/dev/null 2>&1; then git config core.hooksPath .githooks; fi",
    "prepare": "bun run hooks:install",
    "prepack": "bun run build",
    "verify": "bun run typecheck && bun run test:coverage && bun run build"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.15",
    "@openhoo/hooversion": "0.1.1",
    "@types/bun": "^1.3.14",
    "@types/node": "^25.9.1",
    "tsup": "^8.5.1",
    "typescript": "^7.0.0"
  }
}
